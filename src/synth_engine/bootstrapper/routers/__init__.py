"""Route modules for the Conclave Engine bootstrapper.

Each submodule defines an :class:`fastapi.APIRouter` that is included in
the main application via :func:`synth_engine.bootstrapper.main.create_app`.

Task: P5-T5.1 — Task Orchestration API Core
"""
