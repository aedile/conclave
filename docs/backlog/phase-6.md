# Phase 6: Integration, Audit & Finalization

**Goal:** Validate all components working in unison, enforce NIST erasure, and prepare for handover.

---

## Task 6.1: Execute E2E Generative Synthesis Subsystem Tests
**Assignee:** [Dev B]
**Priority:** Blocked by 3.5, 4.3, 4.4, 5.4
**Estimated Effort:** 3 days

### User Story / Agentic Goal
As a Release Manager, I want full end-to-end integration tests confirming the Generative Synthesis workflow (frontend through to deep-learning backend) works correctly, so that we can guarantee the entire platform is stable before handing it over to the client.

### Context & Constraints
*   Running real PyTorch models in CI/CD takes too long. Must use a "Dummy ML Model" that generates random assertions but exercises the exact same API, Storage, and Privacy Ledger pathways as the real model.

### Acceptance Criteria
*   [ ] Write a comprehensive E2E suite utilizing Playwright.
*   [ ] the test navigates the React UI, uploads a dummy schema, and initiates a generation job.
*   [ ] The test verifies the UI correctly receives SSE progress updates and displays completion.
*   [ ] The test asserts the Privacy Ledger (Epsilon) was correctly decremented in the database.
*   [ ] The test runs `@axe-core/playwright` accessibility assertions across the entire job configuration and completion flow.

### Testing & Quality Gates
*   These are the quality gates. The tests must pass consistently in CI without flake.

### Files to Create/Modify
*   [NEW] `tests/integration/test_e2e_synthesis.py`
*   [NEW] `tests/fixtures/dummy_ml_synthesizer.py`

### Definition of Done (DoD) Checklist
1. **Architectural Compliance:** E2E tests validate all tiers (UI, API, ML proxy, DB).
2. **Coverage Gate:** N/A (this is the test).
3. **Pipeline Green:** CI passes.
4. **Peer Review:** Reviewed.
5. **Acceptance Verification:** Acceptance criteria met.

---

## Task 6.2: Execute NIST Erasure & Fuzz Testing Validation
**Assignee:** [Dev C]
**Priority:** Blocked by 5.5
**Estimated Effort:** 4 days

### User Story / Agentic Goal
As a Security Auditor, I want automated validation that cryptographic shredding completely obliterates data according to NIST 800-88, and I want the system fuzzed against edge cases, so that we can certify the platform as mathematically secure.

### Context & Constraints
*   Must validate the "Zeroize" functionality built in Phase 5.
*   Must validate JSON limits mandated by PM review to prevent CPU exhaustion attacks.

### Acceptance Criteria
*   [ ] Create a test that seeds PII data, calls the `/shred` UI/API, and then attempts full filesystem and database memory-scraping (using `pg_stat_file` or similar hacks) to prove the raw data is unrecoverable.
*   [ ] Create fuzz tests against the task orchestration API sending massively nested JSON payloads (depth > 100) to ensure the API rejects them immediately with a `413 Payload Too Large` or `400 Bad Request` rather than causing a stack overflow.
*   [ ] Create fuzz tests sending `NaN` and `Infinity` float values to the ML Profiler endpoints to ensure they are handled gracefully rather than crashing the Python process.
*   [ ] Run an automated OWASP ZAP baseline scan against the configured container stack.

### Testing & Quality Gates
*   All automated validation tests must pass locally and in the CI pipeline.

### Files to Create/Modify
*   [NEW] `tests/security/test_nist_erasure.py`
*   [NEW] `tests/security/test_fuzzing.py`
*   [MODIFY] `.github/workflows/ci.yml` (add OWASP Zap step)

### Definition of Done (DoD) Checklist
1. **Architectural Compliance:** Security mandates proven via automated testing.
2. **Coverage Gate:** N/A.
3. **Pipeline Green:** CI passes.
4. **Peer Review:** Reviewed.
5. **Acceptance Verification:** Acceptance criteria met.

---

## Task 6.3: Final Security Remediation & Documentation
**Assignee:** [Dev D]
**Priority:** Blocked by 6.2
**Estimated Effort:** 4 days

### User Story / Agentic Goal
As a Technical Writer / Developer, I want to resolve any findings from the final security audits and generate clean, comprehensive user guides, so that the customer can operate the dark-room platform safely without external support.

### Context & Constraints
*   All documentation must be generated locally (Markdown or offline HTML).
*   Must prioritize the "Vault Unseal" and "Disaster Recovery" runbooks.

### Acceptance Criteria
*   [ ] Remediate any findings generated by the OWASP or Fuzz testing.
*   [ ] Write the `OPERATOR_MANUAL.md` detailing how to boot the server, manage the `docker-compose.yml`, and unseal the vault.
*   [ ] Write the `DISASTER_RECOVERY.md` detailing how to recover a failed subsetting job (Saga rollback manual intervention if necessary) or what happens in an OOM event.
*   [ ] Write the `LICENSING.md` detailing the offline activation protocol.
*   [ ] Verify the Air-Gap bundler (`make build-airgap-bundle`) includes all of this generated documentation.

### Testing & Quality Gates
*   Verify markdown linting (`markdownlint`) passes on all new documentation.
*   Review documentation for clarity and completeness.

### Files to Create/Modify
*   [NEW] `docs/OPERATOR_MANUAL.md`
*   [NEW] `docs/DISASTER_RECOVERY.md`
*   [NEW] `docs/LICENSING.md`
*   [MODIFY] `scripts/build_airgap.sh`

### Definition of Done (DoD) Checklist
1. **Architectural Compliance:** Documentation is offline-ready and comprehensive.
2. **Coverage Gate:** N/A.
3. **Pipeline Green:** CI passes (markdownlint).
4. **Peer Review:** Reviewed.
5. **Acceptance Verification:** Acceptance criteria met.
